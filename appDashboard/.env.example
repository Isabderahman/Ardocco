#
# Public runtime config (exposed to the browser)
#

# Google Maps JavaScript API key (restrict by HTTP referrers in Google Cloud Console)
NUXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Backend API
#
# Option A (recommended for SSR/dev): set the backend base URL for Nuxt server routes to proxy requests
NUXT_BACKEND_BASE_URL=http://localhost:8000
# If your Laravel server runs on another port (example):
# NUXT_BACKEND_BASE_URL=http://localhost:8002
#
# Option B (for static hosting): call the backend API directly from the browser (requires CORS on backend)
NUXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
# Example:
# NUXT_PUBLIC_API_BASE_URL=http://localhost:8002/api
